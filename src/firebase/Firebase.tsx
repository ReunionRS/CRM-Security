// Firebase removed: project migrated to PostgreSQL backend API.
// File left intentionally for backward compatibility with old imports.
export const auth = null;
export const firestoreBase = null;
export const storage = null;
export const adminAuth = null;
